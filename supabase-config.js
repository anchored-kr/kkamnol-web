// Supabase 설정 — 대시보드 > Project Settings > API 에서 복사해 붙여넣기.
// anon key는 "공개 클라이언트 키"라 정적 JS에 넣어도 안전(RLS가 권한 강제).
// 비어 있으면 애널리틱스(누적 플레이/공유)는 조용히 비활성(no-op).
export const SUPABASE_URL = "";        // 예: https://abcdxyz.supabase.co
export const SUPABASE_ANON_KEY = "";   // 예: eyJhbGciOi... (anon public)
